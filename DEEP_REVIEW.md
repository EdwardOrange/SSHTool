# 深度代码审查与修复记录

日期：2026-09-14。基于本轮开始时已有的未提交改动继续审查，保留原有 UI 改造和测试。本文只记录本轮新增修复；此前记录见 `REVIEW.md`。

审查重点为 Rust SSH/SFTP、防火墙变更、主机信任、SQLite 更新和前端转发状态。修复直接保存在工作区，未提交 Git，也未操作用户配置的远程服务器。

## 已修复问题

| 优先级 | 原问题与触发条件 | 修复及验证 |
| --- | --- | --- |
| P1 | firewalld 中 TCP/UDP 全端口、ICMP 规则没有生成协议条件，允许规则可能扩大为全协议放行 | 显式生成 protocol 条件，IPv6 使用 ipv6-icmp；有端口时要求 TCP/UDP；回归覆盖协议条件 |
| P1 | UFW/firewalld 忽略目标地址；双栈规则遇到单栈来源可能部分执行失败 | 校验 IP/CIDR 并保留目标地址；按地址族交集生成规则；转换端口列表及范围，拒绝不兼容输入 |
| P1 | firewalld backendRef 能携带未验证的 shell 片段 | 在生成命令入口统一校验，并要求普通端口引用与端口/协议字段完全一致 |
| P1 | 回滚快照和 at 确认标记位于可由其他账号写入的 /tmp 路径 | 使用完整随机 UUID 的 /run 私有目录、0700 权限及 077 umask；不允许复用预建目录 |
| P1 | 提交、执行与定时回滚之间存在竞争；准备成功但未修改时，回滚也可能恢复旧快照 | 服务器端使用同一 flock 锁；校验服务器时钟期限并记录 mutation-started、rollback-started、rolled-back、committed；未修改时不恢复快照，回滚开始后拒绝迟到的执行/提交 |
| P1 | 同一主机可同时应用多个计划，快照回滚可能覆盖另一计划 | 拒绝交叠变更；新计划执行前检查旧计划的服务器端完成标记，不以本机倒计时归零推断回滚已经完成 |
| P1 | 对已有 firewalld 规则重复添加后，逆操作回滚会删除原规则 | 执行前查询规则是否已经存在；重复添加在建立回滚前拒绝 |
| P1 | 下载覆盖同名目录时，会把原目录移到备份位置并以文件替换 | 覆盖和续传仅接受普通文件；最终替换前再次检查；用含原始文件的真实目录验证数据保留 |
| P1 | 导入配置的 hostKeyFingerprint 可绕过首次主机确认；待确认指纹未绑定观察时的地址 | 只接受本地 known_hosts 中明确确认的端点信任；导入时清除指纹；待确认记录同时保存端点；本机 SSH 服务器测试覆盖绕过与地址变更 |
| P2 | SSH 命令通道缺少退出状态时，直接删除整条连接；收到退出状态后过早停止读取 | 保留传输连接，发送输入 EOF，读取到通道关闭，保留退出状态后的输出；增加打开/执行超时和显式关闭；真实 SSH 协议测试验证异常后的下一条命令仍可执行 |
| P2 | 并发连接同一主机可创建多条传输并覆盖会话索引 | 每主机连接/断连串行化，连接和认证有总超时；本机并发测试验证仅建立一条连接 |
| P2 | 防火墙验证新连接时无法复用未保存的 SSH 登录密码 | 仅在会话内用 Zeroizing 保存手动输入的响应，以供重新认证；不写磁盘，随会话释放；不等同于多步骤 MFA 支持 |
| P2 | 远程转发停止失败后记录已删除，无法重试；本地停止返回时监听端口可能尚未释放 | 远程取消成功后再移除任务，失败保留；等待本地监听任务结束；集成测试验证立即重绑、拒绝取消后的重试和完整断连 |
| P2 | 数据库存储的 active 状态在断连后仍显示运行中 | 列表以 SSH 运行任务为准；前端在连接状态变化时重新查询；活动转发禁止直接改配置 |
| P2 | 连接完成时用旧 HostProfile 覆盖连接期间的名称/收藏等修改 | 数据库在锁内只更新连接元数据；不重新插入已删除主机；测试覆盖编辑保留和删除后不复活 |
| P2 | 续传比较假定本地和网络读取每次返回等长数据，合法短读会误判文件不同 | 按固定区间 read_exact 比较，覆盖不同分块、提前 EOF 和实际差异 |
| P2 | 传输源变短或变长仍可能覆盖完整目标；上传续传的单文件进度遗漏已有前缀 | 上传、下载、远程复制在替换前校验传输字节数；失败保留旧目标；进度从续传偏移开始 |
| P2 | 远程复制的相对路径/符号链接祖先可绕过自身后代检查；上传目录扫描可能沿 Windows junction 递归 | 复制前使用服务器 canonicalize 结果重新检查；本地扫描跳过重解析点 |
| P2 | SFTP 用服务器错误文本猜测文件是否不存在，权限错误可能误走创建流程 | 按协议 NoSuchFile 状态码判断，其他错误向上传递；覆盖本地化错误消息与含误导文字的权限错误 |
| P2 | 终端审计初始化写入失败时，会话已加入注册表但没有对应后台任务 | 初始化成功后才登记会话，后台退出时显式关闭通道 |

## 验证结果

- `npm test`：15 项通过，本轮新增 1 项。
- `cargo test --manifest-path src-tauri/Cargo.toml --lib`：47 项通过，本轮新增 17 项。
- 新增 6 项本机 SSH/数据库集成测试，服务器仅绑定 127.0.0.1 随机端口，使用固定测试密钥和测试密码；不会读取用户凭据或运行远程 shell。
- 实际执行了 MSYS Bash/flock 并发测试：提交与回滚的副作用只能发生一方；过期提交、回滚后迟到执行均被拒绝；准备后未发生修改的计划不恢复快照。
- `npm run build`：TypeScript 与生产构建通过。
- `npm run desktop:build -- --no-bundle`：Windows Release 构建通过，产物为 `src-tauri/target/release/ssh-operations-terminal.exe`。
- `cargo clippy --manifest-path src-tauri/Cargo.toml --lib --message-format short`：无错误，保留 8 条参数数量/条件可合并的风格警告。
- `git diff --check`：通过。

Vite 仍提示主入口约 636 KB，超过默认 500 KB 分块阈值；这是现有打包优化事项。Windows 链接器打印生成导入库的提示，不影响构建结果。未生成或签署 MSI 安装包。

## 兼容性与验收边界

防火墙写入现在要求服务器具备 `flock`，以及 `systemd-run` 或 `at`。缺少工具时返回明确错误。采用 at 时，由作业内部 sleep 保证不会因分钟精度提前回滚；调度延迟可能使实际回滚晚于界面期限。界面期限结束后禁止提交，后续计划仍需确认服务器端完成标记。私有回滚目录保留至服务器重启，以便延迟任务检查状态。

实际 Linux UFW/firewalld 变更、sudo 权限组合、系统调度服务异常、服务器重启、真实 SFTP 大文件及 Windows junction 的端到端传输仍需隔离服务器验收。Bash/flock 测试验证生成脚本的互斥与状态判断，不代表已验证真实防火墙工具的全部行为。执行中的防火墙命令若挂起，会持有锁并延迟回滚；客户端超时不能保证远端进程终止。

键盘交互认证仍使用单个响应值，完整多步骤 MFA 未实现。文件传输校验检测大小变化，不提供同长度并发修改的内容一致性保证；本地路径校验不能完全消除其他本地进程在检查后替换路径的竞争。外部管理员同时修改防火墙也不受本应用事务锁约束。

规则语义核对参考：[firewalld rich language 官方文档](https://firewalld.org/documentation/man-pages/firewalld.richlanguage.html)、[Ubuntu UFW 手册](https://manpages.ubuntu.com/manpages/noble/man8/ufw.8.html)。
