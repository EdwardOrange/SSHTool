# 深度代码审查与修复记录

日期：2026-09-20。审查范围包括 React 异步状态、Tauri 命令边界、SSH/跳板机、终端、SFTP、端口转发、监控、防火墙事务及 SQLite 配置导入。以下仅记录本轮新增修复，既有记录见 `REVIEW.md` 和 `DEEP_REVIEW.md`。

## 修复结果

| 优先级 | 问题及触发条件 | 修复与回归依据 |
| --- | --- | --- |
| P1 | firewalld 多条永久化命令中途失败，原回滚只恢复运行时，残留永久规则会在 reload/重启后生效 | 执行前独立查询各条永久规则原状态，保存私有恢复脚本；失败回滚恢复永久与运行时，任一恢复失败均不标记完成；真实 Bash/flock 脚本测试覆盖部分提交失败 |
| P1 | 回滚通过启动 systemd service 返回时，恢复操作可能尚未结束；未完整执行的计划也可能被提交 | 主动回滚同步运行相同加锁脚本；只有 apply-completed 才能提交，重复 apply 被拒绝，已完成 commit 可幂等返回；脚本测试覆盖互斥和失败状态 |
| P1 | SFTP 服务端目录项可以返回 `../outside` 或绝对路径，递归操作可能越过选中目录 | 列表、复制、下载、删除统一校验单个 POSIX 文件名；删除在处理当前批次前验证全部子项；内存 SFTP 协议测试证明异常批次不会删除文件 |
| P2 | 主机连接期间保存新地址/身份，数据库与实际会话可能不一致；迟到编辑或连接可复活已删除主机，递归跳板机绕过界面层锁 | 保存、删除、信任操作与 SSH 连接共享主机锁；等锁后重新读取数据库并校验跳板链；现有 ID 的保存必须仍有记录；本机 SSH 和数据库测试覆盖删除、换地址、换用户与循环跳板 |
| P2 | 转发启动等待期间删除/改写配置，完成响应会重新写回过期记录；前端迟到启动结果覆盖断连后的停止状态 | 命令层串行化转发数据库及运行任务变更，与主机删除/断连协调；前端忽略旧生命周期响应，加载及删除期间限制冲突操作 |
| P2 | 终端收到 ExitStatus 就停止读取，丢失其后的尾部输出；输入队列满时关闭请求也被阻塞 | 读取至通道关闭，独立取消信号中断排队和阻塞写入；本机 SSH 测试覆盖尾部输出，满队列回归覆盖关闭完成 |
| P2 | 新增 firewalld 规则把 zone 固定为 public，而界面读取的是服务器默认 zone | 未指定 zone 时使用 firewall-cmd 默认 zone，保留显式 zone；生成命令测试覆盖默认及指定区域 |
| P2 | 监控重启共用旧基线，旧任务清理/并发注册可能影响新任务，系统时钟调整影响网络速率 | 任务内独立基线、单锁管理任务与主机索引、单调时钟计算速率；旧任务清理不影响新任务 |
| P2 | 监控异常数字、缺失字段被当作零或溢出，解析失败无审计；非标准采样间隔不属于数据库清理范围 | 核心字段严格验证，计数检查溢出，异常样本保留错误审计且不污染基线；统一支持 2/5/10/30 秒间隔 |
| P2 | SFTP 切目录等待/失败仍残留旧目录，右键操作和重命名可能使用错误路径；关闭错误提示后又允许写入 | 清空旧列表和操作弹窗，成功列出目录后才开放操作，重命名从源文件父目录计算；回归覆盖加载失败与面包屑导航 |
| P2 | 多次设置保存失败回滚到未保存的中间值，恢复默认时的新编辑被覆盖，不同设置入口互相覆盖 | 统一串行补丁队列，以最近成功结果为基准重新应用待保存修改，保留后端规范化；回归覆盖失败、重置和不同入口交叠 |
| P2 | 前端连接完成使用旧主机对象整条覆盖，丢失期间编辑或重建已删除主机 | 仅更新最新存在主机的连接状态及连接时间；状态仓库回归验证编辑保留和删除后不复活 |
| P2 | 配置导入先查询再 upsert，并发写入可被导入旧配置覆盖，返回结果还包含未插入记录 | 在 SQLite 事务内 INSERT ... ON CONFLICT DO NOTHING，只返回实际新增记录；验证重复导入不覆盖现有地址与名称 |
| P2 | SOCKS5 动态转发拒绝 IPv6 地址类型 | 实现 ATYP 4 解析；本机 socket 测试验证 IPv6、端口和后续负载边界 |

## 验证

- `npm test`：24/24 通过，本轮新增 9 项。
- `cargo test --manifest-path src-tauri/Cargo.toml --lib`：68/68 通过，本轮新增 21 项。
- 本轮共新增 30 项回归，前后端合计 92 项测试通过。
- SSH 测试仅使用 127.0.0.1 随机端口和固定测试凭据；SFTP 异常目录测试使用内存协议服务。
- 防火墙 Bash/flock 脚本测试实际执行，未跳过；命令副作用使用临时目录中的标记模拟。
- `npm run build`：TypeScript 和 Vite 生产构建通过。
- `cargo clippy --manifest-path src-tauri/Cargo.toml --lib --tests --message-format short`：无错误；仍有原有 8 条参数数量/条件合并风格警告。
- `npm run desktop:build -- --no-bundle`：Windows Release 构建通过，产物为 `src-tauri/target/release/ssh-operations-terminal.exe`。链接器的导入库生成提示不影响构建。
- `git diff --check`：通过。修改保存在工作区，未提交 Git；未生成或签署 MSI 安装包。

## 验收边界

本轮没有连接用户配置的远程服务器，没有更改真实防火墙、读取真实登录凭据或执行远程文件写入。真实 Linux UFW/firewalld、systemd/at 调度、sudo 权限组合、真实 SSH 跳板链和大型 SFTP 文件仍需要隔离环境端到端验收。新增模拟与协议测试不能替代这些验收。

Vite 主入口仍约 637 KB，超过默认 500 KB 分块提示阈值。既有多步骤 MFA、传输源同长度并发修改、服务器重启/调度故障等限制仍见此前审查记录，本轮不宣称已解决。

firewalld 运行时/永久配置及查询命令语义依据：[官方 firewall-cmd 手册](https://firewalld.org/documentation/man-pages/firewall-cmd.html)。
