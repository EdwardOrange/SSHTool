# 深度审查与实机验证（2026-09-21）

本轮从干净工作区开始，对 React/Tauri/SSH/SFTP/监控/防火墙/审计进行并行审查，并由其他审查者交叉检查改动。使用程序已有、已信任的服务器 `111`，未输出认证口令或私钥内容。本文仅记录本轮实际完成的验证，不沿用此前报告的通过结论。

## 修复

| 问题 | 修复与回归证据 |
| --- | --- |
| 服务器拒绝 exec、PTY、Shell 或 SFTP 请求时，客户端仍可能显示成功或等待很久 | 等待 Success/Failure，明确报错和超时；保留确认前输出，关闭失败通道。真实本地 SSH 协议测试覆盖拒绝与连接继续可用 |
| SFTP 库的类型位包含判断把 socket/block device 误判为目录 | 使用精确文件类型；对已声明的特殊文件拒绝传输、续传、覆盖；缺失类型位的服务器保持兼容。内存 SFTP 协议覆盖 |
| 同服务器复制错误套用 Windows 文件名限制 | 远程复制允许合法 POSIX 文件名 `CON`、`a:b`；下载到 Windows 仍执行本地名称校验 |
| 24 小时/7 天监控历史 API 的数据在一小时后消失 | 持久化一分钟、五分钟桶，保留真实采样时间，阻止迟到样本覆盖，查询按时间段选择分辨率。旧版已清理的数据无法恢复；当前界面仍为实时图，没有新增历史范围控件 |
| 部分日志来源未脱敏，CLI 参数和 JSON 密钥值可能遗漏 | 在数据库写入及实时推送共同边界脱敏；补常用参数/赋值别名、嵌套 JSON 与畸形 JSON；测试验证数据库和广播内容相同 |
| 清空审计可能丢失并发新记录，或被迟到事件、旧快照恢复 | 后端事务返回删除 ID；前端合并清空前可见 ID，并在事件/快照入口过滤已清空 ID；保留期间新增记录。补重复提交与失败反馈测试 |
| 审计历史查询失败后实时事件一直留在缓冲中 | 查询成功或失败都结束缓冲；保留已收到的实时事件 |
| 审计加载失败或长期等待阻止整个应用启动 | 服务器和设置加载独立于审计；审计失败仅提示，不阻塞连接 |
| 隐藏终端仍可弹出粘贴确认，迟到剪贴板内容可能发送到旧会话 | 切换页面作废请求并关闭浮层；验证当前终端实例和活动状态；断开重连重新创建终端 |
| SFTP 操作等待文件选择器或传输启动时能重复提交；旧页面回调影响新页面 | 同步操作锁覆盖完整启动过程；页面代次隔离错误与刷新，保留后台传输进度 |
| 小窗口的审计/传输面板挤占主要操作区 | 审计面板按窗口高度设上限，低高度窗口进一步收紧；传输列表上限为 `min(230px,20vh)`，保留折叠入口 |
| 小窗口展开审计时，SFTP 表格被 flex 收缩到几乎不可见 | 原生 793×631 窗口复现；文件页面改为整体纵向滚动，表格保持内容高度，避免工具栏和面包屑挤掉文件行 |
| 浏览器演示模式的连接状态被轮询恢复为旧值 | 同步演示后端连接/断开状态并回归验证 |
| 默认页面设置遗漏防火墙和端口转发 | 补齐全部五个业务页面选项 |
| WebView 偏好存储不可用会阻断启动或面板操作 | 主题、语言及面板缓存读取/写入容错，主题缓存值校验；数据库设置仍为准 |

## 验证方式

- 常规自动化：前端 Vitest、TypeScript/Vite、Rust 单元/协议/脚本测试、Clippy、diff 检查。
- 实机入口：显式设置 `SSHOPS_LIVE_TEST=1`、`SSHOPS_LIVE_HOST_NAME=111`、`SSHOPS_LIVE_SCOPE=all`，串行运行全部四个 ignored 入口。日常 `cargo test` 不触碰真实服务器。
- 服务器配置和指纹通过只读 SQLite 连接加载，实机测试记录写入临时数据库。
- SFTP 文件测试限定在新建的 `/tmp/sshops-qa-<UUID>`，结束重新连接并验证目录删除。
- UFW 仅创建 UUID 注释、高端口、文档保留来源地址的临时规则，使用独立恢复连接及快照，最后比较原规则状态和两个持久化规则文件 SHA-256。
- 断流测试切断本机 TCP relay 中的测试连接，不停止 sshd、不关闭服务器网卡。
- 原生窗口使用 computer-use 验证；终端命令与远程文件写入通过程序自身 Rust 功能测试执行。

## 验证结果

| 项目 | 本轮结果 |
| --- | --- |
| Vitest | 19 个测试文件、77 项通过，0 失败；较基线 59 项增加 18 项 |
| Rust 常规测试 | 95 项通过，0 失败，4 个实机入口默认忽略；较基线 83 项增加 12 项 |
| 全部实机入口 | 4/4 通过、0 失败，串行一次完整运行 505.77 秒 |
| 命令/PTY | stdin、中文 stdout、stderr、非零退出码；初始尺寸 83×27，调整 101×37；审计开关、退出码 7、尾部输出、会话关闭 |
| SFTP 文件 | 4 MiB + 17 字节二进制和 Unicode 文件，上传下载 SHA-256 一致；ask/skip/rename/overwrite/resume 全路径及错误前缀拒绝 |
| SFTP 目录/取消 | 空目录、零字节文件、递归复制、重命名、删除、防复制到自身后代；8 MiB 源文件实际开始传输后取消，原目标保持不变、临时文件移除 |
| 端口转发 | 本地、SOCKS5、远程回环三种数据往返及停止后监听释放 |
| 断线恢复 | 本机 relay 主动切断后连接状态失效、命令被拒绝，重新认证并恢复命令 |
| 真实监控 | 两次采样、有限指标、进程/socket 解析、内存/磁盘/uptime 与远端原始数据交叉核对、数据库保存 |
| UFW | 基线 18 条规则；计划、应用、新 SSH 验证、手动回滚、提交、过期计划拒绝、精确删除、实际写入后注入失败恢复 |
| UFW 自动恢复 | systemd 观察耗时 63.6 秒、at 61.0 秒（包括轮询/重新连接）；断开测试客户端后恢复，过期提交被拒绝 |
| 远端清理 | 两轮 UFW 原规则状态、user.rules/user6.rules SHA-256 一致；定时任务、快照和临时目录删除；SFTP 隔离目录不存在验证通过 |
| 原生桌面交互 | 已保存配置连接、实时监控、终端提示符、真实 UFW 规则、转发表单必填校验、SFTP 目录导航；深浅色、设置、记录台折叠；最终版在 720×600 客户区（截图 723×631）验证 SFTP 整页滚动、文件行和目录可操作 |
| 生产构建 | TypeScript/Vite 通过；最终 Windows Release 成功，优化构建 52.73 秒，已启动并复核新版程序 |
| 静态检查 | Clippy 无错误，4 项既有参数数量风格提示；git diff --check 通过 |

常规测试合计 **172 项通过**，本轮新增 **30 项**；另有 **4 项实机集成入口通过**。记录文件：`logs/frontend-tests-2026-09-21.log`、`logs/frontend-build-2026-09-21.log`、`logs/live-validation-2026-09-21.log`、`logs/desktop-build-2026-09-21.log`。日志目录按现有 .gitignore 不纳入版本控制。

实机测试的第一次受限环境运行在建立连接前被沙箱拒绝（Windows 10013），获准在沙箱外重跑后完成。第一次受限 Release 构建中的 rustc 异常退出（STATUS_ILLEGAL_INSTRUCTION），沙箱外重试成功；未将这两个环境失败记为功能通过。

可执行文件：`src-tauri/target/release/ssh-operations-terminal.exe`（21,330,944 字节）。SHA-256：`2C2A207B4ECE9231D19AA5F575152B31F70E474ADAA032AF128287B6859E4C3B`。

## Material Design 3

延续项目已有语义配色、深浅色表面层级、字阶、48px 操作目标、圆角和键盘焦点样式。本轮修复小窗口空间分配，核对浅色/深色、设置对话框、页签和记录台折叠。颜色角色对比度回归检查正文及操作文本达到 4.5:1。未把所有 Material 组件都作为已认证的规范实现。

参考：[Google 的 Material 3 主题说明](https://developer.android.com/codelabs/m3-design-theming)、[Material 组件颜色角色](https://github.com/material-components/material-components-android/blob/master/docs/theming/Color.md)。

## 验证边界

- 不能以有限测试保证“零 bug”。此报告区分实机、协议模拟、自动化及人工界面观察。
- 现有服务器使用 Ubuntu/UFW/root 私钥；真实 firewalld、非 root sudo 密码、完整多跳、SSH Agent 账号流程不在这台服务器覆盖范围。
- 多步骤不同回答的 MFA 尚不支持；GB/TB 文件、长时间压力、磁盘耗尽、服务器重启、停止 sshd/网卡未执行。
- 并发修改同长度源文件的一致性、外部管理员同时修改防火墙、远端命令永久挂起等既有限制仍需专门环境验证。
- 脱敏只覆盖明确定义的格式，不代表任意文本中的秘密均可自动识别。
- 界面英文切换仍有部分固定中文文案；本轮未声称完成全量国际化。
- 构建仍有主入口超过 Vite 默认 500 kB 的体积提示；Clippy 有四项既有参数数量风格提示。

修改保留在工作区，未提交 Git。构建目标为 Windows 便携可执行程序，未生成或签署 MSI。
