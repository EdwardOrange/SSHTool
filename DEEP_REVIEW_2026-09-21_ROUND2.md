# 深度审查与最终实机验证：第二轮（2026-09-21）

本轮从干净工作区继续审查已配置的 SSH 运维终端，复用程序中已保存、已信任的服务器 `111`。代码修改保留在工作区，不提交 Git。认证秘密不写入报告。

## 修复内容

| 问题 | 修复及验证 |
| --- | --- |
| 上传、下载、远程复制读取/写入/关闭/安装失败后遗留临时文件 | 统一异常收尾，先释放文件句柄再清理；清理失败明确报告路径及原始错误。内存 SFTP 故障注入覆盖 WRITE/CLOSE/READ/rename 拒绝 |
| 取消不能及时中止挂起的传输，完成瞬间取消仍可能替换目标 | 取消覆盖传输准备、前缀校验和读写；替换前再次检查；替换与原文件恢复完整执行，避免中途遗留备份 |
| 最终安装失败且恢复原文件也失败时，原错误吞掉了备份位置 | 保留原文件备份，返回安装和恢复两种错误及备份路径；回归验证备份内容不变 |
| 取消下载时，阻塞线程可能在清理后才创建 `.part` | 在进入可取消范围前等待文件创建完成，续传使用已打开句柄；回归验证已取消且尚未执行的准备任务释放句柄和清理文件 |
| 数字设置每次按键立即持久化，清空保留天数会提交最小保留值 | 编辑阶段保留草稿，失焦/Enter/切页/关闭时校验整数范围再保存；无效值显示字段错误。防止中间值触发提前清理日志 |
| 慢保存后自动关闭可能丢失用户刚输入的新草稿 | 新编辑取消旧待关闭请求；覆盖保存成功、失败与等待期间再编辑 |
| 修改服务器地址、端口或用户名后继续复用旧认证信息 | 前后端同时清除旧 SSH 凭据引用；sudo 缓存按主机 ID 与端点/用户名散列隔离，不依赖删除旧秘密成功 |
| 主机配置变更后旧防火墙计划可能落到同 ID 的另一端点 | 修改连接身份或删除主机时作废相关计划；防火墙与连接/编辑/删除共用主机操作锁，保留服务器上独立自动回滚 |
| 设置读取中的迁移写回可能覆盖并发保存 | 数据库比较原值后再写回，冲突则重新读取；覆盖并发更新与首次创建 |
| 转发加载失败显示成空列表，启动失败仍显示已停止 | 增加读取失败和重试入口、加载状态、准确错误状态；成功重试清除旧错误，保存后清空表单，同步锁防重复提交 |
| 浏览器演示中的转发开关无效，切页丢配置，设置读取回到默认值 | 为演示接口补齐状态保存、启停、断开联动、删除和设置重置；不模拟真实网络传输 |
| 命令记录复制失败仍提示已复制，备用文本框可能残留 | 检查剪贴板后备命令返回值、异常提示、finally 清理并恢复焦点；移除可能覆盖后续错误的旧提示定时器 |
| 记录台输入标签被容器裁切，记录行操作目标偏小 | 增加工具栏上下留白，将可点击记录行最小高度设为 48px；延续现有 MD3 语义配色、圆角与焦点样式 |

## 自动化结果

- 前端：21 个文件、96 项通过，0 失败；基线 77 项，本轮新增 19 项。
- Rust：109 项通过，0 失败，4 个真实服务器入口默认忽略；基线 95 项，本轮新增 14 项。
- 常规测试合计 **205 项通过**，新增 **33 项**。
- Clippy：无错误，保留 4 项既有函数参数数量风格提示。
- TypeScript/Vite 通过；主入口约 642 kB，仍有超过默认 500 kB 的打包体积提示。

## 实机运行与恢复

最初受限环境在连接前返回 Windows socket 10013，获准执行后可连接。首次长运行在后续会话中断时停止于 SFTP 测试，不能记作整套通过；该次两轮 UFW 日志已记录原规则和持久文件校验一致、定时任务与备份删除。随后使用程序自身 SSH/SFTP 功能、只读加载已保存连接，精确清理日志标识的旧 UUID 隔离目录并核验不存在（独立清理测试 1/1 通过）。清理入口拒绝非规范 UUID、非专属目录、符号链接和非 0700 目录。

最终代码串行完整实机测试 **4/4 通过，0 失败，591.75 秒**，本次没有以分段测试冒充完整运行。

| 项目 | 最终结果 |
| --- | --- |
| SSH/PTY | 已保存私钥、已信任指纹、独立重新认证；stdin、中文 stdout、stderr、退出码；初始尺寸/resize、命令审计开关、尾部输出、会话关闭 |
| SFTP 文件 | 4 MiB + 17 字节二进制和 Unicode 文件往返 SHA-256 一致；上传/下载 ask、skip、rename、overwrite、resume 与错误前缀拒绝 |
| SFTP 目录/取消 | 空目录、零字节文件、递归复制、重命名、删除、禁止复制到后代；8 MiB 源文件实际产生传输进度后取消，原目标保持一致、临时文件移除 |
| 转发 | 本地、SOCKS5、远程回环三种实际数据往返，停止后监听释放 |
| 断流/重连 | 切断本机 TCP relay 中测试连接，识别失效并拒绝旧连接命令，重新认证恢复；不停止服务器服务或网卡 |
| 监控 | 两次真实采样，CPU/内存/磁盘/网络有限值，socket/进程解析，内存、磁盘、uptime 与原始命令交叉核对，SQLite 持久化 |
| UFW 事务 | 18 条原规则基线；计划、应用、新 SSH 验证、手动回滚、提交、旧计划拒绝、精确删除、写入后注入失败恢复 |
| UFW 自动恢复 | systemd 观察到恢复 62.6 秒，at 62.5 秒（包括轮询/重新连接）；测试客户端断开后恢复，过期提交拒绝 |
| 远端恢复 | 两轮原规则状态与 user.rules/user6.rules SHA-256 一致，测试定时器/快照/目录删除；最终 SFTP UUID 目录不存在验证通过 |

## 新版桌面验证与产物

- Windows Release 构建成功，优化构建 54.30 秒；新版已实际启动。
- 原生界面通过 computer-use 验证已保存服务器连接、持续实时监控、终端提示符、18 条真实 UFW 规则、SFTP 根目录到 `/tmp` 导航、转发表单空值校验与刷新入口。
- 实际输入 `100.5` 滚动缓冲行数，关闭时出现整数范围字段错误且不保存；输入 `12000` 后保存、关闭、再次打开显示正确。只读 SQLite 交叉验证持久化成功，结束恢复 `10000`。
- 深浅色界面、记录台工具栏留白与 48px 记录行已查看；窗口缩到截图 729×631（约 726×600 客户区），侧栏收进菜单、工具栏换行、SFTP 整页滚动与目录点击仍可用。没有将初始视口外的文件行误认为丢失。
- 结束恢复原深色主题、滚动缓冲 10000，日志保留天数 7 和容量 100 MB 保持原值；重新打开程序恢复默认窗口尺寸。
- `git diff --check` 通过。未执行提交、发布或生成签署的 MSI。

可执行文件：`src-tauri/target/release/ssh-operations-terminal.exe`，21,376,512 字节。

SHA-256：`EE5D147EC41B0D5DA97C016644A175DA65CE961AC9C8B5989C0EE46A4121B0B0`。

## Material Design 3

沿用项目现有主题与组件规格，重点核对深浅色、48px 交互目标、字段校验反馈、载入/失败状态、对话框与小窗口滚动。依据 [Material Design 3 主题说明](https://developer.android.com/develop/ui/compose/designsystems/material3) 和 [Google 无障碍交互指南](https://developer.android.com/guide/topics/ui/accessibility/apps)。这不是第三方认证，也不代表所有组件细节已穷尽检查。

## 验证边界

有限测试不能保证程序绝对零缺陷。现有实机为 Ubuntu/UFW/root/直接私钥连接；真实 firewalld、非 root sudo 密码、SSH Agent、多跳及多步 MFA不在这台服务器覆盖范围。没有停止 sshd、关闭服务器网卡、重启服务器、填满磁盘或执行 GB/TB 级长时间压力测试。协议故障由本机内存服务器注入，不等同于全部服务器实现与故障组合。

旧版仅按主机 ID 保存的 sudo 缓存不再自动读取，更新后第一次需要 sudo 时须重新输入一次。SSH 已保存配置无需重新录入。全量英文文案仍存在既有限制。本次交互与测试不修改业务文件，远端写入限定隔离目录及可回滚的 UUID 临时规则。

日志保存在被 Git 忽略的 `logs/`：`frontend-tests-round2-final.log`、`rust-tests-round2.log`、`clippy-round2.log`、`interrupted-run-cleanup-round2.log`、`live-validation-round2-final.log`、`desktop-build-round2.log`。
