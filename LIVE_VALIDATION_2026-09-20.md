# 真实服务器功能验证与第二轮修复

日期：2026-09-20。使用程序已配置、已信任的服务器 `111`，通过程序自身 Rust SSH/SFTP/监控/防火墙实现测试。服务器为 Ubuntu 24.04.2，使用 root 私钥认证，UFW 启用，基线为 18 条规则。用户明确授权专用测试机受控中断并恢复。

这份记录接续 `DEEP_REVIEW_2026-09-20.md`。前一份记录中的“未连接真实服务器”描述的是此前代码审查阶段。

## 实机验证

| 功能 | 验证内容和结果 |
| --- | --- |
| 已保存服务器 | 桌面中确认配置并成功连接；Rust 实机测试复用已信任主机指纹和私钥，通过独立新连接再次认证 |
| 命令执行 | stdin、中文 stdout、独立 stderr、非零退出码正常 |
| 交互终端 | PTY 初始 83×27、调整至 101×37，命令审计退出码 7、审计开关、尾部输出、关闭通过 |
| SFTP 文件 | 256 KiB + 17 字节二进制及 Unicode 文件，往返 SHA-256 一致；询问冲突、跳过、重命名、覆盖、续传及不一致前缀拒绝通过 |
| SFTP 目录 | 空目录、零字节文件、递归复制、重命名、删除、禁止复制到自身后代通过 |
| SFTP 取消 | 生成 8 MiB 本地源，在收到实际传输进度后取消；原远端目标内容保持不变，临时文件清除。此项不代表完整传输了 8 MiB |
| 转发 | 本地转发、SOCKS5、远程回环转发，数据往返和停止后监听释放通过；端口释放探针排除了 TCP TIME_WAIT 对检测的干扰 |
| 异常断流恢复 | 本地 TCP relay 承载到真实服务器的连接，主动切断首条连接后 15 秒内检测未连接、拒绝执行命令；重新认证及命令执行恢复，代理监听释放通过。没有修改服务器网络或 SSH 配置 |
| 监控 | 两次真实采样、初始基线、CPU/内存/磁盘/网络有限值、进程与 socket 解析、SQLite 保存；内存总量、根分区容量、uptime 与服务器原始命令交叉核对通过 |
| UFW 事务 | 生成计划、应用、新 SSH 验证、手动回滚、持久提交、旧状态计划拒绝、精确编号删除提交、实际写入后注入失败并同步恢复通过 |
| UFW 自动恢复 | 分别使用 systemd 和已运行的 atd，断开本测试 SSH 客户端后自动恢复；观察耗时 61.8 秒、61.7 秒（含轮询和重连），重连和拒绝到期提交通过。不是关闭服务器网卡或 sshd 服务 |

## 本轮发现并修复

1. **SFTP 下载重命名放错后缀**：`report.csv (1)` 改为 `report (1).csv`；补冲突命名回归。
2. **远程文件修改时间全部显示空值**：使用 SFTP 返回的 mtime，并保留缺失时间的真实状态；协议测试覆盖有/无时间。
3. **审计实时记录不完整**：监控和防火墙写入数据库后未进入统一界面流。改为数据库成功持久化后统一通知，并在释放 SQLite 锁后通知；前端先订阅、缓冲，再合并历史，避免启动窗口丢记录。
4. **终端关闭仍可能等待前端输入队列**：关闭立即取消后端会话，卸载后丢弃尚未发送的输入。
5. **命令详情隐藏 stderr**：同时显示 stdout 与 stderr。
6. **带转义和拼接的敏感值脱敏不完整**：覆盖引号拼接、转义空格、Unicode 后缀和尾部反斜杠，防止泄漏被截断后的剩余部分。
7. **监控连接数被详情上限截断**：总数与最多 200 条详情分离；没有 PID 的 socket 显示“无进程信息”。
8. **防火墙读取错误被结尾成功命令掩盖**：传播权限/执行错误，避免把失败当作空规则；检测真实可用调度能力。
9. **firewalld 规则展示不完整**：解析各 zone 的端口、服务、rich/direct 规则，限制不能可靠删除的项目；本机回归验证，真实服务器未安装 firewalld。
10. **自动回滚计时可能额外延迟**：systemd 明确 1 秒精度、关闭随机延迟；at 按事务截止时间等待，避免排队后再额外睡满 60 秒。
11. **加密私钥无法从界面输入口令**：新增可选口令和安全保存；遇到加密私钥挑战才询问，未加密私钥直接连接。离线真实 SSH 协议测试覆盖缺少/正确/错误口令和重新认证。
12. **凭据随认证配置变化被误复用**：认证方式或私钥路径变化时清除旧凭据关联；空输入保留同一配置的已保存凭据。新凭据使用独立 ID，数据库更新失败不覆盖旧凭据。
13. **服务器意外断开后界面仍显示已连接**：新增连接状态同步，避免迟到结果覆盖手动重连、编辑和删除。
14. **普通用户无法在读取/规划阶段输入 sudo 密码**：补全读取、计划到应用的口令传递和错误重试；成功后才按用户选择保存。
15. **界面操作与提示缺陷**：标签输入保留逗号；SFTP 修改操作防重复提交并在弹窗显示错误；防火墙计划防重复、忽略旧页面响应、验证失败禁止提交。提交和回滚也使用操作锁与页面代次校验，sudo 失效后仅重试用户当前步骤。

## 隔离与恢复

- 实际应用数据库仅以只读方式加载配置和已信任指纹，操作记录、指标和测试配置写入临时数据库；未输出私钥内容、登录口令。
- SFTP 写入限定于每次新建的 `/tmp/sshops-qa-<UUID>`，测试结束验证目录已删除。
- UFW 仅使用 UUID 备注、高端口和文档保留来源地址 `192.0.2.1/32` 创建测试规则；使用独立恢复连接和配置快照，不修改原 SSH 放行规则。
- UFW 每轮结束比较原规则状态 hash，以及 `/etc/ufw/user.rules`、`user6.rules` 的 SHA-256；清除本轮定时任务、私有工作目录和备份。
- 实机测试默认 `#[ignore]`，另需显式设置 `SSHOPS_LIVE_TEST=1`，普通 `cargo test` 不会触碰服务器。Windows 上共享测试 exe，实机测试与重新编译应串行执行。

## 最终回归

- `cargo test --manifest-path src-tauri/Cargo.toml --lib`：83 项通过、0 项失败，另有 4 项实机入口默认忽略。
- 3 项监控/UFW 实机测试显式运行全部通过，最终一轮 216.59 秒，含 systemd 和 at 两种回滚路径。
- SSH 综合实机测试在修复下载命名后通过传输及终端部分；最后的转发端口释放探针误报修正后，转发定向复验 18.78 秒通过，异常断流定向复验 26.09 秒通过。没有把这些记录冒充为修改后整套无中断重跑。
- 新增 sudo 认证分类和单次 stdin 提权读取脚本测试已包含在 83 项通过结果中。
- Clippy 无错误，仅剩 4 项既有函数参数数量风格警告。
- `npm test`：59 项通过、0 项失败。前后端常规测试合计 **142 项通过**，比前一轮的 92 项增加 50 项。
- TypeScript 和 Vite 生产构建通过。Vite 主入口 639.70 kB，仍有超过默认 500 kB 的分块提示。
- `npm run desktop:build -- --no-bundle`：Windows Release 构建成功，Rust 优化构建耗时 48.28 秒。产物为 `src-tauri/target/release/ssh-operations-terminal.exe`；链接器仅提示生成导入库。
- 关闭占用旧可执行文件的本项目进程后完成构建，已重新启动新版程序进程。桌面窗口和操作结果仍须与下述桌面控制限制区分。
- `git diff --check` 通过。

## 验收边界

- 不能据此承诺程序绝对没有 bug；本报告仅承诺列明的步骤、结果和恢复检查。
- 桌面连接、监控、终端和目录浏览已实际查看；随后桌面控制工具收到物理 Esc 停止。用户授权继续后工具仍返回停止状态，因此修改后的完整桌面点击复测尚未完成，不能计为通过。
- 当前服务器没有 firewalld。sudo 密码分支用本机脚本/协议和前端测试验证，未创建非 root 账号或修改服务器 sudoers。
- 未覆盖服务器重启、停止 sshd、关闭网卡、磁盘耗尽、GB/TB 传输、长时间压力、完整多跳真实服务器链、Windows SSH Agent 的真实账号流程及所有系统版本。
- 键盘交互目前仅支持单个响应值，不支持多个不同回答的多步骤 MFA；界面已说明。传输检测大小变化，不能保证其他进程并发修改同长度内容时的一致性。
- 外部管理员同时修改防火墙不受本应用事务锁约束；远端防火墙命令本身挂起可能延迟持锁的回滚任务。

修改保存在工作区，未提交 Git。构建目标为 Windows 可执行程序，未生成或签署 MSI 安装包。
