# 代码审查与 Material 3 界面改造记录

审查日期：2026-09-14。范围覆盖 React 界面、Zustand 状态、Tauri 命令、SSH/终端生命周期、监控采样、SFTP 路径与传输、防火墙操作、端口转发、凭据和设置。改动已直接落实到当前工作区。

| 问题及影响 | 本次修复 |
| --- | --- |
| 切换服务器后仍可能显示旧 SFTP/转发请求结果，操作弹窗沿用旧主机上下文 | 按主机隔离页面实例；增加请求序号、effect 清理、过期响应检查；传输结束刷新当前目录 |
| 未连接服务器仍请求远程文件及防火墙数据 | 增加断连状态提示和操作入口保护 |
| 远端连接已经关闭，但本地会话表仍被当作在线 | 检查 SSH handle 的关闭状态；再次连接前清理旧资源；关闭失效连接时避免重复发送命令 |
| 隐藏终端仍可能获得键盘焦点；字号变化未同步远端尺寸 | 隐藏面板使用 inert；仅激活终端获得焦点；订阅 xterm resize，同步 PTY 行列数 |
| 终端初始化、尺寸更新、关闭和剪贴板操作可能产生未处理拒绝 | 增加异常处理及延迟初始化后的资源清理 |
| 采样停止后仍持续显示“实时” | 独立时钟检查数据时效，显示等待采样或异常状态 |
| CPU 统计重复计入 guest / guest_nice | 总量仅累加前八项，避免虚拟化负载被重复计入 |
| 监控停止需要等待 SSH 命令返回，旧任务清理可能影响新任务索引 | 在采样等待中处理取消；持有同一写锁判断及清理任务归属 |
| 防火墙回滚期限结束后无法关闭弹窗 | 提供“关闭并刷新状态”；超时禁用提交；提交/回滚期间禁用重复操作 |
| 防火墙规则开关没有对应处理逻辑，造成可编辑错觉 | 改为明确的只读启用状态标签 |
| SFTP 自动重命名可能误把父目录的点号当扩展名 | 仅处理文件 basename；保留父目录和隐藏文件语义 |
| 下载路径检查遗漏 Windows junction 和悬空链接 | 使用 symlink_metadata，Windows 检查 REPARSE_POINT 标志；读取失败时返回错误 |
| 远程复制的自包含判断可被重复斜杠及点路径绕过 | 规范化路径后判断自身及后代目录，禁止复制根目录 |
| 远程删除可接受根目录和当前目录别名 | 执行前校验整批路径，拒绝根目录、当前目录及父目录跳转 |
| 表单接受小数端口、空私钥路径等无效输入 | 前端校验整数端口、必填项及单个文件名；后端校验认证类型及私钥路径 |
| 切换认证方式后可能错误保存密码 | 仅密码认证路径允许保存 SSH 密码 |
| 转发停止失败后界面强制显示已停止 | 保留操作前状态并显示错误，避免误报 |
| 语言保存失败缺少错误反馈；取消保存主机操作仍可关闭弹窗 | 增加保存失败反馈和语言回退；保存期间禁用取消 |
| 已删除主机的监控、防火墙缓存仍驻留 | 删除主机时同步清理缓存 |
| 已结束且总量为零的传输仍显示无限进度动画 | 结束后使用确定进度；取消错误可见；停止显示无意义 ETA |

界面依据 [Material 3](https://m3.material.io/) 的色彩角色、排版、形状、状态和自适应布局统一设计。使用 MUI 组件并定制主题，并非仅启用其默认样式。

- 深浅主题采用 primary/onPrimary、container/onContainer、surface、outline 等语义配色。
- 统一 Material 字号阶梯、28px 对话框圆角、胶囊按钮、描边输入框和 M3 开关。
- 主要按钮、图标按钮与开关提供至少 48px 高度的交互区域；补充焦点、名称和键盘操作。
- 宽窗口使用常驻服务器列表，较窄窗口使用抽屉；功能页签可滚动，表格在自身容器内横向滚动。
- 监控卡片响应式排列，图表采用主题色、明确图例与可读坐标轴；保留减少动画偏好。
- 命令记录台首次使用默认收起，已有显式展开偏好仍保留；可用键盘调整高度。
- 终端首次访问时才加载及创建，切换页面保持已创建的终端会话。
- Windows 最小窗口调整为 720×600；浏览器还检查了 600px 宽窗口下的布局。
- 浏览器预览明确标记演示数据。

性能结果：图标改为按模块导入，构建转换模块数从 12,322 降至 1,590；主入口脚本由约 1,005 KB 降至 636 KB，gzip 约 199 KB。终端约 370 KB，监控约 373 KB，按需加载。主入口仍超过 Vite 默认 500 KB 分块提示阈值，该提示未被关闭。

验证：

- npm test：14 项通过（原有 6 项，新增 8 项）。
- cargo test --manifest-path src-tauri/Cargo.toml --lib：30 项通过（原有 25 项，新增 5 项）。
- npm run build：TypeScript 检查及正式前端构建通过。
- Windows Release 构建：npm run desktop:build -- --no-bundle。
- 浏览器检查了深浅主题、宽屏与 1000px/600px 窗口、监控、设置和终端；生产预览已观察到的页面未记录控制台错误。
- 新增回归覆盖过期监控、延迟启动清理、跨主机请求隔离、断连 SFTP、防火墙超时关闭、配色对比度、路径改名/复制/删除、下载路径和 CPU 统计。

关键文本配色组合的自动检查满足 4.5:1 对比度；这不是整套应用的无障碍认证，也不能代替真实设备及屏幕阅读器验收。

验证边界：未使用真实 SSH 服务器执行远程写操作。网络异常恢复、实际 PTY 行列变化、Windows junction 的端到端下载拦截、UFW/firewalld 的服务器端自动回滚、真实文件传输冲突及多轮 MFA，仍需在隔离测试服务器上验收。当前键盘交互认证沿用单一响应值，并不构成完整的多步骤 MFA 交互。本次产物为便携可执行文件，未生成或签署 MSI 安装包。

设计参考：[Material 3](https://m3.material.io/)、[官方 Material 3 主题指南](https://developer.android.com/codelabs/m3-design-theming)。监控字段参考：[Linux /proc 文档](https://www.kernel.org/doc/html/latest/filesystems/proc.html)。
